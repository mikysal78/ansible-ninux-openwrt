---
# inventory/group_vars/build_hosts.yml
#
# NON modificare questo file.
# Tutte le variabili di configurazione sono in ninux.yml (root del repo).
# I segreti (shared_secret openwisp) sono in inventory/vault.yml (cifrato).
